
'use client';

// This component has been temporarily disabled due to a persistent bug.
export default function StudyRoomClient({ roomId, currentUser }: { roomId: string; currentUser: any }) {
    return (
        <div className="flex items-center justify-center h-full">
            <p>This feature is currently undergoing maintenance. Please check back later.</p>
        </div>
    );
}
